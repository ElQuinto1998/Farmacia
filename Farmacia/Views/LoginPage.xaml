<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Farmacia.Views.LoginPage">
    <Grid>
        <BoxView Color="#3FA5E8" Opacity="0.6"/>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Text="TeDamosLaBienvenidaAFarmacia"
                   Margin="20"
                   TextColor="White"
                   FontSize="Large"
                   FontFamily="Montserrat"
                   FontAttributes="Bold"
                VerticalOptions="EndAndExpand" 
                HorizontalOptions="Center" />
            <StackLayout Orientation="Vertical">
                <Image Source="" HeightRequest="120" Margin="40"/>
            </StackLayout>
            <StackLayout Grid.Row="1" Padding="30,50" Margin="10">
                <Entry Placeholder="Usuario" PlaceholderColor="White" x:Name="email"/>
                <Entry Placeholder="Contraseña" PlaceholderColor="White" IsPassword="True" x:Name="password" />
                <Button Text="IniciarSesion" BackgroundColor="White" BorderRadius="20" TextColor="#3FA5E8" Clicked="HomePage_Click" />
                <StackLayout Orientation="Horizontal">
                    <Label Text="RecuperarContraseña"  TextColor="Black" FontAttributes="Bold"/>
                    <Label Text="Registrarme" HorizontalOptions="EndAndExpand" TextColor="Black" FontAttributes="Bold" />
                </StackLayout>
            </StackLayout>
        </Grid>
    </Grid>
</ContentPage>