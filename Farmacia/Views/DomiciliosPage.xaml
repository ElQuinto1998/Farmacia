<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Farmacia.Views.DomiciliosPage">
    <StackLayout>
        <ListView x:Name="ListaMedicamentos" HasUnevenRows="True" ItemSelected="ListaMedicamentos_ItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal" Padding="10">
                            <StackLayout Orientation="Vertical">
                                <Label Text="{Binding name, StringFormat='Nombre: {0}'}" TextColor="Gray"></Label>
                                <Label Text="{Binding salePrice, StringFormat='Precio: ${0}'}" TextColor="Red"></Label>
                            </StackLayout>
                            <Image Source="medi.png" HeightRequest="30" WidthRequest="50" HorizontalOptions="EndAndExpand"></Image>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Text="Continuar" Clicked="Button_Clicked" BackgroundColor="#909CD1" Padding="10"/>
    </StackLayout>
</ContentPage>